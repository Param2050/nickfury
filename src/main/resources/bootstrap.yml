spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nick
  profiles: dev
server:
  address: 127.0.0.1

---

spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nick
  profiles: uat
server:
  address: 127.0.0.1

---

spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nick
  profiles: prod
server:
  address: 127.0.0.1
