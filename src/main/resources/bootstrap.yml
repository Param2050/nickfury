spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nickfury
  profiles: dev

---

spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nickfury
  profiles: uat

---

spring:
  cloud:
    config:
      uri: http://localhost:9091
  application:
    name: nickfury
  profiles: prod
